{
  "name": "{{NAME}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "wrangler": "^4.129.0"
  }
}
